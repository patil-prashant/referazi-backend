<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="apis.mapper.UserProfileMapper">
    <resultMap id="UserProfileResultMap" type="apis.models.UserProfile">
        <id column="id" property="id" />
        <result column="user_id" property="userId"/>
        <result column="job_title" property="jobTitle" />
        <result column="company" property="company" />
        <result column="location" property="location" />
    </resultMap>
    <select id="getUserProfile" resultMap="UserProfileResultMap">
        select * from user_profile where user_id=#{userId}
    </select>
    <select id="findUserProfileById" resultMap="UserProfileResultMap">
        select id, skill, email from user_profile WHERE id=#{id}
    </select>
    <insert id="insertUser" parameterType="apis.models.UserProfile" useGeneratedKeys="true" keyProperty="id">
        insert into user_profile(user_id,job_title,company,location) values(#{userId},#{jobTitle},#{company},#{location})
    </insert>
</mapper>