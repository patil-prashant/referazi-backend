<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.referazi.dao.UserProfileDao">
    <resultMap id="UserProfileResultMap" type="com.referazi.models.UserProfile">
        <result column="user_id" property="userId"/>
        <result column="job_title" property="jobTitle" />
        <result column="company" property="company" />
        <result column="location" property="location" />
        <result column="role" property="role"/>
    </resultMap>
    <select id="getAllUserProfiles" resultMap="UserProfileResultMap">
        select * from user_profile where user_id=#{userId}
    </select>
    <select id="findUserProfileByRole" resultMap="UserProfileResultMap">
        select * from user_profile WHERE user_id=#{userId} and role=#{role}
    </select>
    <insert id="saveUserProfile" parameterType="com.referazi.models.UserProfile">
        insert into user_profile(user_id,job_title,company,location,role) values(#{userId},#{jobTitle},#{company},#{location},#{role})
    </insert>
</mapper>